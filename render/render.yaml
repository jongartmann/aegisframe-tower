services:
  - type: web
    name: aegisframe-tower
    runtime: rust
    buildCommand: cargo build --release -p aegisframe-render
    startCommand: ./target/release/aegisframe-render
    rootDir: ..
    envVars:
      - key: RUST_LOG
        value: info
